spring:
  application:
    name: authentication

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
    default-schema: auth

  datasource:
    url: jdbc:postgresql://localhost:5432/auth?currentSchema=auth
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        default_schema: auth
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      # TODO: configure username and password in container
#      user: root
#      password:  root
      database: 0
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

application:
  jwt:
    access-expire-time-sec: 18000 # 5 hours
    refresh-expire-time-sec: 86400 # 1 day