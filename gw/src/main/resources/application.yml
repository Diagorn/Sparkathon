spring:
  application:
    name: gateway

  main:
    web-application-type: reactive

  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/auth-service/**
          filters:
            - StripPrefix=1

jwt:
  secret: f4dea871a7569cba13a3f56f0b4ea9a3

grpc:
  server:
    port: 9091
    in-process-name: gw-grpc